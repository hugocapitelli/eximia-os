# PRP-06: Configurações

**Produto**: Agenda Cheia - Growth Layer para Salões de Beleza
**Módulo**: Configurações e Gerenciamento de Conta
**Data**: Janeiro 2025
**Versão**: 1.0

---

## 1. Objetivo

Criar uma interface completa de configurações que permita profissionais gerenciarem sua conta, conexão WhatsApp, horários de atendimento, ciclos de serviço, equipe, assinatura e preferências de privacidade - tudo em um design mobile-first e intuitivo.

### Princípios de Design

1. **Organização Clara**: Seções bem definidas com navegação fácil
2. **Segurança em Primeiro Lugar**: Confirmação para ações críticas (desconectar WhatsApp, deletar conta)
3. **Transparência**: Status claro de conexões e assinatura
4. **Self-Service**: Profissional resolve problemas sem suporte
5. **LGPD Compliant**: Controle total sobre dados pessoais

---

## 2. Wireframes

### 2.1 Tela Principal - Menu de Configurações

```
┌─────────────────────────────────────┐
│  ← Configurações                    │
├─────────────────────────────────────┤
│                                     │
│  👤 CONTA                           │
│  ┌───────────────────────────────┐ │
│  │ Carla Souza                   │ │
│  │ carla@salaodiva.com           │ │
│  │ Salão Diva                    │ │
│  │                          →    │ │
│  └───────────────────────────────┘ │
│                                     │
│  📱 WHATSAPP                        │
│  ┌───────────────────────────────┐ │
│  │ ✅ Conectado                  │ │
│  │ (11) 98765-4321               │ │
│  │ Última sincronização: agora   │ │
│  │                          →    │ │
│  └───────────────────────────────┘ │
│                                     │
│  💼 NEGÓCIO                         │
│  ┌───────────────────────────────┐ │
│  │ ⏰ Horários de Atendimento    │ │
│  │    Seg-Sex: 9h-19h        →  │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ 💅 Ciclos de Serviço          │ │
│  │    6 serviços configurados →  │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ 👥 Profissionais              │ │
│  │    Só você por enquanto   →  │ │
│  └───────────────────────────────┘ │
│                                     │
│  💳 ASSINATURA                      │
│  ┌───────────────────────────────┐ │
│  │ 🎯 Desafio 5 Clientes         │ │
│  │ 4/5 completos - Falta 1!  →  │ │
│  └───────────────────────────────┘ │
│                                     │
│  🔔 NOTIFICAÇÕES                    │
│  ┌───────────────────────────────┐ │
│  │ Push, Email, WhatsApp     →  │ │
│  └───────────────────────────────┘ │
│                                     │
│  🔒 PRIVACIDADE & DADOS             │
│  ┌───────────────────────────────┐ │
│  │ LGPD, Exportar, Deletar   →  │ │
│  └───────────────────────────────┘ │
│                                     │
│  📚 SUPORTE                         │
│  ┌───────────────────────────────┐ │
│  │ Ajuda                     →  │ │
│  └───────────────────────────────┘ │
│  ┌───────────────────────────────┐ │
│  │ Termos de Uso             →  │ │
│  └───────────────────────────────┘ │
│  ┌───────────────────────────────┐ │
│  │ Política de Privacidade   →  │ │
│  └───────────────────────────────┘ │
│                                     │
│  🚪 Sair                            │
│                                     │
│  v1.0.0                             │
│                                     │
├─────────────────────────────────────┤
│  [📊] [👥] [💬] [⚙️]               │
└─────────────────────────────────────┘
```

### 2.2 Editar Conta

```
┌─────────────────────────────────────┐
│  ← Minha Conta                      │
├─────────────────────────────────────┤
│                                     │
│  ┌───────────────────────────────┐ │
│  │                               │ │
│  │         [ Foto ]              │ │
│  │                               │ │
│  │      📷 Alterar foto          │ │
│  └───────────────────────────────┘ │
│                                     │
│  Nome Completo                      │
│  ┌───────────────────────────────┐ │
│  │ Carla Souza                   │ │
│  └───────────────────────────────┘ │
│                                     │
│  Nome do Salão                      │
│  ┌───────────────────────────────┐ │
│  │ Salão Diva                    │ │
│  └───────────────────────────────┘ │
│                                     │
│  Email                              │
│  ┌───────────────────────────────┐ │
│  │ carla@salaodiva.com           │ │
│  └───────────────────────────────┘ │
│                                     │
│  Telefone (opcional)                │
│  ┌───────────────────────────────┐ │
│  │ (11) 3456-7890                │ │
│  └───────────────────────────────┘ │
│                                     │
│  Endereço                           │
│  ┌───────────────────────────────┐ │
│  │ Rua das Flores, 123           │ │
│  └───────────────────────────────┘ │
│  ┌──────────┐  ┌────────────────┐  │
│  │ Bairro   │  │ Cidade         │  │
│  └──────────┘  └────────────────┘  │
│                                     │
│  ┌─────────────────────────────┐   │
│  │  Salvar Alterações          │   │
│  └─────────────────────────────┘   │
│                                     │
│  ─────────────────────────────────  │
│                                     │
│  🔐 Segurança                       │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ Alterar Senha             →  │ │
│  └───────────────────────────────┘ │
│                                     │
├─────────────────────────────────────┤
│  [📊] [👥] [💬] [⚙️]               │
└─────────────────────────────────────┘
```

### 2.3 Conexão WhatsApp

```
┌─────────────────────────────────────┐
│  ← WhatsApp                         │
├─────────────────────────────────────┤
│                                     │
│  Status da Conexão                  │
│                                     │
│  ┌───────────────────────────────┐ │
│  │                               │ │
│  │       ✅ Conectado            │ │
│  │                               │ │
│  │   (11) 98765-4321             │ │
│  │                               │ │
│  │   Última sincronização:       │ │
│  │   agora                       │ │
│  │                               │ │
│  │   ✓ Recebendo mensagens       │ │
│  │   ✓ Enviando mensagens        │ │
│  │   ✓ Bot ativo                 │ │
│  │                               │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌─────────────────────────────┐   │
│  │  🔄 Reconectar              │   │
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │  🔌 Desconectar             │   │
│  └─────────────────────────────┘   │
│                                     │
│  ─────────────────────────────────  │
│                                     │
│  ℹ️ Sobre a Conexão                 │
│                                     │
│  • Você pode ter apenas 1 WhatsApp  │
│    conectado por vez                │
│                                     │
│  • Ao desconectar, o bot para de    │
│    funcionar imediatamente          │
│                                     │
│  • Reconecte escaneando novo QR     │
│    code sempre que precisar         │
│                                     │
│  • Sua conta WhatsApp continua      │
│    funcionando normalmente no       │
│    celular                          │
│                                     │
├─────────────────────────────────────┤
│  [📊] [👥] [💬] [⚙️]               │
└─────────────────────────────────────┘
```

### 2.4 Horários de Atendimento

```
┌─────────────────────────────────────┐
│  ← Horários de Atendimento          │
├─────────────────────────────────────┤
│                                     │
│  Configure quando você atende       │
│  para que o bot só sugira           │
│  horários disponíveis.              │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ ☑️ Segunda-feira              │ │
│  │   09:00 — 19:00               │ │
│  │   [Editar]                    │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ ☑️ Terça-feira                │ │
│  │   09:00 — 19:00               │ │
│  │   [Editar]                    │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ ☑️ Quarta-feira               │ │
│  │   09:00 — 19:00               │ │
│  │   [Editar]                    │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ ☑️ Quinta-feira               │ │
│  │   09:00 — 19:00               │ │
│  │   [Editar]                    │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ ☑️ Sexta-feira                │ │
│  │   09:00 — 19:00               │ │
│  │   [Editar]                    │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ ☐ Sábado                      │ │
│  │   Fechado                     │ │
│  │   [Editar]                    │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ ☐ Domingo                     │ │
│  │   Fechado                     │ │
│  │   [Editar]                    │ │
│  └───────────────────────────────┘ │
│                                     │
│  🏖️ Férias / Ausências              │
│  ┌───────────────────────────────┐ │
│  │ Configurar períodos       →  │ │
│  └───────────────────────────────┘ │
│                                     │
├─────────────────────────────────────┤
│  [📊] [👥] [💬] [⚙️]               │
└─────────────────────────────────────┘
```

### 2.5 Ciclos de Serviço

```
┌─────────────────────────────────────┐
│  ← Ciclos de Serviço                │
├─────────────────────────────────────┤
│                                     │
│  Defina a cada quantos dias cada    │
│  cliente deve voltar para cada      │
│  serviço.                           │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ 💅 Manicure                   │ │
│  │                               │ │
│  │ Ciclo de retorno: 21 dias     │ │
│  │ ─────●─────────────────       │ │
│  │ 7d           30d         90d  │ │
│  │                               │ │
│  │ Cliente deve voltar a cada    │ │
│  │ 3 semanas                     │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ 🦶 Pedicure                   │ │
│  │ Ciclo: 21 dias            [✏️]│ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ 💇‍♀️ Cabelo                    │ │
│  │ Ciclo: 35 dias            [✏️]│ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ 💈 Barba                      │ │
│  │ Ciclo: 15 dias            [✏️]│ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ 🌊 Escova                     │ │
│  │ Ciclo: 7 dias             [✏️]│ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ ✨ Progressiva                │ │
│  │ Ciclo: 90 dias            [✏️]│ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌─────────────────────────────┐   │
│  │  + Adicionar Serviço        │   │
│  └─────────────────────────────┘   │
│                                     │
├─────────────────────────────────────┤
│  [📊] [👥] [💬] [⚙️]               │
└─────────────────────────────────────┘
```

### 2.6 Assinatura e Cobrança

```
┌─────────────────────────────────────┐
│  ← Assinatura                       │
├─────────────────────────────────────┤
│                                     │
│  Status Atual                       │
│                                     │
│  ┌───────────────────────────────┐ │
│  │  🎯 Desafio 5 Clientes        │ │
│  │                               │ │
│  │  ████████░░ 4/5               │ │
│  │                               │ │
│  │  Você recuperou 4 clientes!   │ │
│  │  Falta apenas 1 para          │ │
│  │  desbloquear modo pago.       │ │
│  │                               │ │
│  │  💰 R$ 49/mês depois          │ │
│  └───────────────────────────────┘ │
│                                     │
│  🎁 O que vem depois?               │
│                                     │
│  • Recalls ilimitados               │
│  • Suporte prioritário              │
│  • Analytics avançado               │
│  • Integração com agenda            │
│  • Remarketing automático           │
│                                     │
│  ─────────────────────────────────  │
│                                     │
│  💳 Método de Pagamento             │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ Nenhum cadastrado             │ │
│  │                               │ │
│  │ Adicione quando completar     │ │
│  │ o desafio                     │ │
│  │                               │ │
│  │ [Adicionar Cartão]            │ │
│  └───────────────────────────────┘ │
│                                     │
│  📜 Histórico de Faturas            │
│  Nenhuma ainda                      │
│                                     │
├─────────────────────────────────────┤
│  [📊] [👥] [💬] [⚙️]               │
└─────────────────────────────────────┘
```

### 2.7 Notificações

```
┌─────────────────────────────────────┐
│  ← Notificações                     │
├─────────────────────────────────────┤
│                                     │
│  Escolha como quer ser notificado   │
│                                     │
│  📱 PUSH NOTIFICATIONS              │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ Novas confirmações      [🟢]  │ │
│  │ Sempre que um cliente         │ │
│  │ solicitar agendamento         │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ Recalls recuperados     [🟢]  │ │
│  │ Quando cliente confirmar      │ │
│  │ retorno                       │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ Novas mensagens         [⚪]  │ │
│  │ Quando cliente enviar nova    │ │
│  │ mensagem no WhatsApp          │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ Resumo diário           [🟢]  │ │
│  │ Todo dia às 19h com resumo    │ │
│  │ do que aconteceu              │ │
│  └───────────────────────────────┘ │
│                                     │
│  📧 EMAIL                           │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ Relatório semanal       [🟢]  │ │
│  │ Toda segunda às 9h            │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ Atualizações produto    [🟢]  │ │
│  │ Novidades e melhorias         │ │
│  └───────────────────────────────┘ │
│                                     │
│  💬 WHATSAPP                        │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ Alertas importantes     [🟢]  │ │
│  │ Problemas de conexão,         │ │
│  │ limite atingido, etc.         │ │
│  └───────────────────────────────┘ │
│                                     │
│  🔕 Modo Silencioso                 │
│  ┌───────────────────────────────┐ │
│  │ Horário: Desativado       [⚪]│ │
│  │ Pausar notificações em        │ │
│  │ horários específicos          │ │
│  └───────────────────────────────┘ │
│                                     │
├─────────────────────────────────────┤
│  [📊] [👥] [💬] [⚙️]               │
└─────────────────────────────────────┘
```

### 2.8 Privacidade & Dados (LGPD)

```
┌─────────────────────────────────────┐
│  ← Privacidade & Dados              │
├─────────────────────────────────────┤
│                                     │
│  🔒 Seus Dados                      │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ 📊 Exportar Meus Dados        │ │
│  │                               │ │
│  │ Baixe todos os seus dados     │ │
│  │ em formato JSON               │ │
│  │                          →    │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ 🗑️ Solicitar Exclusão         │ │
│  │                               │ │
│  │ Deletar permanentemente sua   │ │
│  │ conta e dados                 │ │
│  │                          →    │ │
│  └───────────────────────────────┘ │
│                                     │
│  ─────────────────────────────────  │
│                                     │
│  🛡️ LGPD                            │
│                                     │
│  Estamos em conformidade com a      │
│  Lei Geral de Proteção de Dados     │
│  (LGPD - Lei 13.709/2018).          │
│                                     │
│  Seus dados são:                    │
│  • Criptografados                   │
│  • Armazenados no Brasil            │
│  • Nunca compartilhados             │
│  • Deletáveis a qualquer momento    │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ Política de Privacidade   →  │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ Termos de Uso             →  │ │
│  └───────────────────────────────┘ │
│                                     │
│  ─────────────────────────────────  │
│                                     │
│  📞 Contato DPO                     │
│  dpo@agendacheia.com                │
│                                     │
├─────────────────────────────────────┤
│  [📊] [👥] [💬] [⚙️]               │
└─────────────────────────────────────┘
```

### 2.9 Modal de Confirmação - Desconectar WhatsApp

```
┌─────────────────────────────────────┐
│                                     │
│        ⚠️ Desconectar WhatsApp?     │
│                                     │
│  Isso vai:                          │
│                                     │
│  • Pausar o bot imediatamente       │
│  • Parar envio de recalls           │
│  • Desativar confirmações           │
│                                     │
│  Você pode reconectar a qualquer    │
│  momento escaneando novo QR code.   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │  Sim, Desconectar           │   │
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │  Cancelar                   │   │
│  └─────────────────────────────┘   │
│                                     │
└─────────────────────────────────────┘
```

### 2.10 Modal de Confirmação - Deletar Conta

```
┌─────────────────────────────────────┐
│                                     │
│        🗑️ Deletar Conta?            │
│                                     │
│  ⚠️ ATENÇÃO: Esta ação é            │
│  PERMANENTE e IRREVERSÍVEL          │
│                                     │
│  Será deletado:                     │
│  • Sua conta                        │
│  • Todos os clientes                │
│  • Histórico de conversas           │
│  • Métricas e relatórios            │
│  • Conexão WhatsApp                 │
│                                     │
│  Recomendamos exportar seus dados   │
│  antes de continuar.                │
│                                     │
│  Digite "DELETAR" para confirmar:   │
│  ┌───────────────────────────────┐ │
│  │                               │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌─────────────────────────────┐   │
│  │  Confirmar Exclusão         │   │
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │  Cancelar                   │   │
│  └─────────────────────────────┘   │
│                                     │
└─────────────────────────────────────┘
```

---

## 3. Especificações Técnicas

### 3.1 Tipos TypeScript

```typescript
// types/settings.ts

export interface UserSettings {
  account: AccountSettings;
  whatsapp: WhatsAppSettings;
  business: BusinessSettings;
  subscription: SubscriptionSettings;
  notifications: NotificationSettings;
  privacy: PrivacySettings;
}

export interface AccountSettings {
  userId: string;
  name: string;
  email: string;
  phone?: string;
  businessName: string;
  address?: {
    street: string;
    neighborhood: string;
    city: string;
    state: string;
    zipCode: string;
  };
  profilePhotoUrl?: string;
}

export interface WhatsAppSettings {
  isConnected: boolean;
  phoneNumber?: string;
  lastSyncAt?: Date;
  status: 'connected' | 'disconnected' | 'error';
  qrCode?: string; // Para reconexão
}

export interface BusinessSettings {
  workingHours: WorkingHours[];
  serviceCycles: ServiceCycle[];
  professionals: Professional[];
  vacations: VacationPeriod[];
}

export interface WorkingHours {
  dayOfWeek: 0 | 1 | 2 | 3 | 4 | 5 | 6; // 0 = Sunday
  isOpen: boolean;
  openTime?: string; // "09:00"
  closeTime?: string; // "19:00"
}

export interface ServiceCycle {
  id: string;
  serviceType: string;
  cycleDays: number;
  estimatedDuration?: number; // minutos
}

export interface Professional {
  id: string;
  name: string;
  role: 'owner' | 'employee';
  services: string[]; // IDs de ServiceCycle
  isActive: boolean;
}

export interface VacationPeriod {
  id: string;
  startDate: Date;
  endDate: Date;
  reason?: string;
}

export interface SubscriptionSettings {
  plan: 'trial' | 'challenge' | 'paid';
  status: 'active' | 'canceled' | 'past_due';
  challengeProgress: number; // 0-5
  nextBillingDate?: Date;
  amount?: number;
  paymentMethod?: PaymentMethod;
  invoices: Invoice[];
}

export interface PaymentMethod {
  type: 'credit_card' | 'pix';
  last4?: string;
  brand?: string;
  expiryMonth?: number;
  expiryYear?: number;
}

export interface Invoice {
  id: string;
  date: Date;
  amount: number;
  status: 'paid' | 'pending' | 'failed';
  pdfUrl?: string;
}

export interface NotificationSettings {
  push: {
    newConfirmations: boolean;
    recallsRecovered: boolean;
    newMessages: boolean;
    dailySummary: boolean;
    dailySummaryTime?: string; // "19:00"
  };
  email: {
    weeklyReport: boolean;
    productUpdates: boolean;
  };
  whatsapp: {
    criticalAlerts: boolean;
  };
  quietHours: {
    enabled: boolean;
    startTime?: string; // "22:00"
    endTime?: string; // "08:00"
  };
}

export interface PrivacySettings {
  dataExportRequested: boolean;
  deletionRequested: boolean;
  gdprConsent: boolean;
  gdprConsentDate?: Date;
}
```

### 3.2 Componente Principal - Configurações

```tsx
// src/screens/SettingsScreen.tsx

import React, { useState, useEffect } from 'react';
import { useNavigate } from 'react-router-dom';
import {
  User,
  MessageCircle,
  Clock,
  Scissors,
  Users,
  CreditCard,
  Bell,
  Shield,
  HelpCircle,
  FileText,
  LogOut,
  ChevronRight
} from 'lucide-react';
import { UserSettings } from '../types/settings';

export function SettingsScreen() {
  const navigate = useNavigate();
  const [settings, setSettings] = useState<UserSettings | null>(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    fetchSettings();
  }, []);

  async function fetchSettings() {
    try {
      const response = await fetch('/api/v1/settings', {
        headers: {
          'Authorization': `Bearer ${localStorage.getItem('token')}`
        }
      });
      const data = await response.json();
      setSettings(data);
    } catch (error) {
      console.error('Error fetching settings:', error);
    } finally {
      setLoading(false);
    }
  }

  async function handleLogout() {
    if (confirm('Tem certeza que deseja sair?')) {
      localStorage.removeItem('token');
      navigate('/login');
    }
  }

  if (loading || !settings) {
    return <SettingsLoadingSkeleton />;
  }

  return (
    <div className="min-h-screen bg-gray-50 pb-20">
      {/* Header */}
      <div className="bg-white border-b border-gray-200 px-4 py-4">
        <h1 className="text-xl font-bold text-gray-900">Configurações</h1>
      </div>

      <div className="px-4 py-6 space-y-6">
        {/* Conta */}
        <section>
          <h2 className="text-sm font-semibold text-gray-500 uppercase mb-3">
            👤 Conta
          </h2>
          <button
            onClick={() => navigate('/settings/account')}
            className="w-full bg-white rounded-xl p-4 shadow-sm border border-gray-100 hover:border-gray-200 transition-colors"
          >
            <div className="flex items-center gap-3">
              <div className="w-12 h-12 rounded-full bg-purple-100 flex items-center justify-center">
                {settings.account.profilePhotoUrl ? (
                  <img
                    src={settings.account.profilePhotoUrl}
                    alt={settings.account.name}
                    className="w-12 h-12 rounded-full object-cover"
                  />
                ) : (
                  <User className="w-6 h-6 text-purple-600" />
                )}
              </div>
              <div className="flex-1 text-left">
                <p className="font-semibold text-gray-900">{settings.account.name}</p>
                <p className="text-sm text-gray-600">{settings.account.email}</p>
                <p className="text-sm text-gray-500">{settings.account.businessName}</p>
              </div>
              <ChevronRight className="w-5 h-5 text-gray-400" />
            </div>
          </button>
        </section>

        {/* WhatsApp */}
        <section>
          <h2 className="text-sm font-semibold text-gray-500 uppercase mb-3">
            📱 WhatsApp
          </h2>
          <button
            onClick={() => navigate('/settings/whatsapp')}
            className="w-full bg-white rounded-xl p-4 shadow-sm border border-gray-100 hover:border-gray-200 transition-colors"
          >
            <div className="flex items-center gap-3">
              <MessageCircle className="w-6 h-6 text-green-600" />
              <div className="flex-1 text-left">
                <div className="flex items-center gap-2">
                  <span className="font-semibold text-gray-900">
                    {settings.whatsapp.isConnected ? '✅ Conectado' : '❌ Desconectado'}
                  </span>
                </div>
                {settings.whatsapp.phoneNumber && (
                  <p className="text-sm text-gray-600">{settings.whatsapp.phoneNumber}</p>
                )}
                {settings.whatsapp.lastSyncAt && (
                  <p className="text-xs text-gray-500">
                    Última sincronização: {formatRelativeTime(settings.whatsapp.lastSyncAt)}
                  </p>
                )}
              </div>
              <ChevronRight className="w-5 h-5 text-gray-400" />
            </div>
          </button>
        </section>

        {/* Negócio */}
        <section>
          <h2 className="text-sm font-semibold text-gray-500 uppercase mb-3">
            💼 Negócio
          </h2>
          <div className="space-y-3">
            <SettingsItem
              icon={<Clock className="w-5 h-5 text-blue-600" />}
              label="Horários de Atendimento"
              subtitle={getWorkingHoursSummary(settings.business.workingHours)}
              onClick={() => navigate('/settings/working-hours')}
            />
            <SettingsItem
              icon={<Scissors className="w-5 h-5 text-purple-600" />}
              label="Ciclos de Serviço"
              subtitle={`${settings.business.serviceCycles.length} serviços configurados`}
              onClick={() => navigate('/settings/service-cycles')}
            />
            <SettingsItem
              icon={<Users className="w-5 h-5 text-orange-600" />}
              label="Profissionais"
              subtitle={
                settings.business.professionals.length === 1
                  ? 'Só você por enquanto'
                  : `${settings.business.professionals.length} profissionais`
              }
              onClick={() => navigate('/settings/professionals')}
            />
          </div>
        </section>

        {/* Assinatura */}
        <section>
          <h2 className="text-sm font-semibold text-gray-500 uppercase mb-3">
            💳 Assinatura
          </h2>
          <button
            onClick={() => navigate('/settings/subscription')}
            className="w-full bg-gradient-to-br from-purple-50 to-blue-50 rounded-xl p-4 shadow-sm border-2 border-purple-200 hover:border-purple-300 transition-colors"
          >
            <div className="flex items-center gap-3">
              <div className="text-3xl">🎯</div>
              <div className="flex-1 text-left">
                <p className="font-semibold text-gray-900">Desafio 5 Clientes</p>
                <p className="text-sm text-purple-700">
                  {settings.subscription.challengeProgress}/5 completos
                  {settings.subscription.challengeProgress < 5 &&
                    ` - Falta ${5 - settings.subscription.challengeProgress}!`
                  }
                </p>
              </div>
              <ChevronRight className="w-5 h-5 text-gray-400" />
            </div>
          </button>
        </section>

        {/* Notificações */}
        <section>
          <h2 className="text-sm font-semibold text-gray-500 uppercase mb-3">
            🔔 Notificações
          </h2>
          <SettingsItem
            icon={<Bell className="w-5 h-5 text-yellow-600" />}
            label="Push, Email, WhatsApp"
            onClick={() => navigate('/settings/notifications')}
          />
        </section>

        {/* Privacidade */}
        <section>
          <h2 className="text-sm font-semibold text-gray-500 uppercase mb-3">
            🔒 Privacidade & Dados
          </h2>
          <SettingsItem
            icon={<Shield className="w-5 h-5 text-green-600" />}
            label="LGPD, Exportar, Deletar"
            onClick={() => navigate('/settings/privacy')}
          />
        </section>

        {/* Suporte */}
        <section>
          <h2 className="text-sm font-semibold text-gray-500 uppercase mb-3">
            📚 Suporte
          </h2>
          <div className="space-y-3">
            <SettingsItem
              icon={<HelpCircle className="w-5 h-5 text-blue-600" />}
              label="Ajuda"
              onClick={() => navigate('/help')}
            />
            <SettingsItem
              icon={<FileText className="w-5 h-5 text-gray-600" />}
              label="Termos de Uso"
              onClick={() => window.open('https://agendacheia.com/termos', '_blank')}
            />
            <SettingsItem
              icon={<FileText className="w-5 h-5 text-gray-600" />}
              label="Política de Privacidade"
              onClick={() => window.open('https://agendacheia.com/privacidade', '_blank')}
            />
          </div>
        </section>

        {/* Sair */}
        <button
          onClick={handleLogout}
          className="w-full bg-white rounded-xl p-4 shadow-sm border border-gray-100 hover:border-red-200 hover:bg-red-50 transition-colors flex items-center gap-3"
        >
          <LogOut className="w-5 h-5 text-red-600" />
          <span className="font-semibold text-red-600">Sair</span>
        </button>

        {/* Versão */}
        <p className="text-center text-sm text-gray-400">v1.0.0</p>
      </div>
    </div>
  );
}

function SettingsItem({
  icon,
  label,
  subtitle,
  onClick
}: {
  icon: React.ReactNode;
  label: string;
  subtitle?: string;
  onClick: () => void;
}) {
  return (
    <button
      onClick={onClick}
      className="w-full bg-white rounded-xl p-4 shadow-sm border border-gray-100 hover:border-gray-200 transition-colors flex items-center gap-3"
    >
      {icon}
      <div className="flex-1 text-left">
        <p className="font-medium text-gray-900">{label}</p>
        {subtitle && <p className="text-sm text-gray-600">{subtitle}</p>}
      </div>
      <ChevronRight className="w-5 h-5 text-gray-400" />
    </button>
  );
}

function getWorkingHoursSummary(hours: WorkingHours[]): string {
  const openDays = hours.filter(h => h.isOpen);
  if (openDays.length === 0) return 'Não configurado';

  const firstDay = openDays[0];
  return `${getDayName(firstDay.dayOfWeek)}-${getDayName(openDays[openDays.length - 1].dayOfWeek)}: ${firstDay.openTime}-${firstDay.closeTime}`;
}

function getDayName(day: number): string {
  const names = ['Dom', 'Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb'];
  return names[day];
}

function formatRelativeTime(date: Date): string {
  const seconds = Math.floor((new Date().getTime() - new Date(date).getTime()) / 1000);

  if (seconds < 60) return 'agora';
  if (seconds < 3600) return `${Math.floor(seconds / 60)}min atrás`;
  if (seconds < 86400) return `${Math.floor(seconds / 3600)}h atrás`;
  return `${Math.floor(seconds / 86400)}d atrás`;
}
```

### 3.3 Editar Horários de Atendimento

```tsx
// src/screens/WorkingHoursScreen.tsx

import React, { useState } from 'react';
import { WorkingHours } from '../types/settings';
import { Clock } from 'lucide-react';

export function WorkingHoursScreen() {
  const [workingHours, setWorkingHours] = useState<WorkingHours[]>([
    { dayOfWeek: 0, isOpen: false },
    { dayOfWeek: 1, isOpen: true, openTime: '09:00', closeTime: '19:00' },
    { dayOfWeek: 2, isOpen: true, openTime: '09:00', closeTime: '19:00' },
    { dayOfWeek: 3, isOpen: true, openTime: '09:00', closeTime: '19:00' },
    { dayOfWeek: 4, isOpen: true, openTime: '09:00', closeTime: '19:00' },
    { dayOfWeek: 5, isOpen: true, openTime: '09:00', closeTime: '19:00' },
    { dayOfWeek: 6, isOpen: false },
  ]);

  const [editingDay, setEditingDay] = useState<number | null>(null);

  const dayNames = [
    'Domingo',
    'Segunda-feira',
    'Terça-feira',
    'Quarta-feira',
    'Quinta-feira',
    'Sexta-feira',
    'Sábado',
  ];

  function toggleDay(dayOfWeek: number) {
    setWorkingHours(prev =>
      prev.map(h =>
        h.dayOfWeek === dayOfWeek
          ? {
              ...h,
              isOpen: !h.isOpen,
              openTime: h.isOpen ? undefined : '09:00',
              closeTime: h.isOpen ? undefined : '19:00',
            }
          : h
      )
    );
  }

  function updateTime(dayOfWeek: number, field: 'openTime' | 'closeTime', value: string) {
    setWorkingHours(prev =>
      prev.map(h =>
        h.dayOfWeek === dayOfWeek ? { ...h, [field]: value } : h
      )
    );
  }

  async function saveWorkingHours() {
    try {
      const response = await fetch('/api/v1/settings/working-hours', {
        method: 'PUT',
        headers: {
          'Authorization': `Bearer ${localStorage.getItem('token')}`,
          'Content-Type': 'application/json',
        },
        body: JSON.stringify({ workingHours }),
      });

      if (response.ok) {
        showSuccessToast('Horários salvos!');
      }
    } catch (error) {
      console.error('Error saving:', error);
      showErrorToast('Erro ao salvar');
    }
  }

  return (
    <div className="min-h-screen bg-gray-50 pb-20">
      {/* Header */}
      <div className="bg-white border-b border-gray-200 px-4 py-4">
        <h1 className="text-xl font-bold text-gray-900">Horários de Atendimento</h1>
        <p className="text-sm text-gray-600 mt-1">
          Configure quando você atende para que o bot só sugira horários disponíveis
        </p>
      </div>

      <div className="p-4 space-y-3">
        {workingHours.map(day => (
          <div
            key={day.dayOfWeek}
            className="bg-white rounded-xl p-4 shadow-sm border border-gray-100"
          >
            <div className="flex items-center gap-3 mb-3">
              <input
                type="checkbox"
                checked={day.isOpen}
                onChange={() => toggleDay(day.dayOfWeek)}
                className="w-5 h-5 text-purple-600 rounded"
              />
              <label className="font-semibold text-gray-900 flex-1">
                {dayNames[day.dayOfWeek]}
              </label>
            </div>

            {day.isOpen ? (
              <div className="flex items-center gap-3 ml-8">
                <input
                  type="time"
                  value={day.openTime || ''}
                  onChange={(e) => updateTime(day.dayOfWeek, 'openTime', e.target.value)}
                  className="border border-gray-300 rounded-lg px-3 py-2 text-sm"
                />
                <span className="text-gray-500">—</span>
                <input
                  type="time"
                  value={day.closeTime || ''}
                  onChange={(e) => updateTime(day.dayOfWeek, 'closeTime', e.target.value)}
                  className="border border-gray-300 rounded-lg px-3 py-2 text-sm"
                />
              </div>
            ) : (
              <p className="text-sm text-gray-500 ml-8">Fechado</p>
            )}
          </div>
        ))}

        <button
          onClick={saveWorkingHours}
          className="w-full bg-purple-600 hover:bg-purple-700 text-white font-semibold py-4 rounded-xl transition-colors mt-6"
        >
          Salvar Horários
        </button>
      </div>
    </div>
  );
}
```

### 3.4 Modal de Desconexão WhatsApp

```tsx
// src/components/DisconnectWhatsAppModal.tsx

import React from 'react';
import { AlertTriangle, X } from 'lucide-react';

interface DisconnectWhatsAppModalProps {
  onConfirm: () => void;
  onCancel: () => void;
}

export function DisconnectWhatsAppModal({ onConfirm, onCancel }: DisconnectWhatsAppModalProps) {
  return (
    <div className="fixed inset-0 bg-black/50 flex items-center justify-center p-4 z-50">
      <div className="bg-white rounded-3xl max-w-md w-full p-6 animate-scale-in">
        <div className="flex items-center justify-between mb-6">
          <AlertTriangle className="w-8 h-8 text-orange-500" />
          <button onClick={onCancel} className="text-gray-400 hover:text-gray-600">
            <X className="w-6 h-6" />
          </button>
        </div>

        <h2 className="text-xl font-bold text-gray-900 mb-4">
          Desconectar WhatsApp?
        </h2>

        <div className="mb-6">
          <p className="text-gray-700 mb-3">Isso vai:</p>
          <ul className="space-y-2 text-gray-700">
            <li className="flex items-start gap-2">
              <span className="text-red-500">•</span>
              Pausar o bot imediatamente
            </li>
            <li className="flex items-start gap-2">
              <span className="text-red-500">•</span>
              Parar envio de recalls
            </li>
            <li className="flex items-start gap-2">
              <span className="text-red-500">•</span>
              Desativar confirmações
            </li>
          </ul>
          <p className="text-sm text-gray-600 mt-4">
            Você pode reconectar a qualquer momento escaneando novo QR code.
          </p>
        </div>

        <div className="space-y-3">
          <button
            onClick={onConfirm}
            className="w-full bg-red-500 hover:bg-red-600 text-white font-semibold py-4 rounded-xl transition-colors"
          >
            Sim, Desconectar
          </button>
          <button
            onClick={onCancel}
            className="w-full bg-gray-100 hover:bg-gray-200 text-gray-700 font-semibold py-4 rounded-xl transition-colors"
          >
            Cancelar
          </button>
        </div>
      </div>
    </div>
  );
}
```

---

## 4. API Backend

### 4.1 Endpoints

```typescript
// Backend API Endpoints

// Buscar todas as configurações
GET /api/v1/settings
Response: UserSettings

// Atualizar conta
PUT /api/v1/settings/account
Body: Partial<AccountSettings>
Response: { success: boolean }

// Desconectar WhatsApp
POST /api/v1/settings/whatsapp/disconnect
Response: { success: boolean }

// Obter QR Code para reconexão
GET /api/v1/settings/whatsapp/qr
Response: { qrCode: string }

// Atualizar horários
PUT /api/v1/settings/working-hours
Body: { workingHours: WorkingHours[] }
Response: { success: boolean }

// Atualizar ciclos de serviço
PUT /api/v1/settings/service-cycles
Body: { serviceCycles: ServiceCycle[] }
Response: { success: boolean }

// Atualizar notificações
PUT /api/v1/settings/notifications
Body: Partial<NotificationSettings>
Response: { success: boolean }

// Exportar dados (LGPD)
POST /api/v1/settings/export-data
Response: { downloadUrl: string }

// Solicitar exclusão de conta (LGPD)
POST /api/v1/settings/delete-account
Body: { confirmation: string } // "DELETAR"
Response: { success: boolean, scheduledFor: Date }
```

---

## 5. Critérios de Sucesso

### 5.1 Métricas de UX
- **Settings Load Time**: <1.5s
- **Save Settings Response**: <500ms
- **WhatsApp QR Code Generation**: <3s
- **Data Export Generation**: <30s

### 5.2 Métricas de Negócio
- **Settings Completion Rate**: >80% dos usuários completam configurações iniciais
- **WhatsApp Reconnection Rate**: <5% de desconexões acidentais
- **Support Ticket Reduction**: 60% de redução em tickets relacionados a configurações
- **LGPD Compliance**: 100% de conformidade com requisitos

---

## 6. Testes

```typescript
// __tests__/SettingsScreen.test.tsx

describe('SettingsScreen', () => {
  it('deve renderizar todas as seções', () => {
    render(<SettingsScreen />);

    expect(screen.getByText(/Conta/i)).toBeInTheDocument();
    expect(screen.getByText(/WhatsApp/i)).toBeInTheDocument();
    expect(screen.getByText(/Negócio/i)).toBeInTheDocument();
    expect(screen.getByText(/Assinatura/i)).toBeInTheDocument();
  });

  it('deve mostrar modal de confirmação ao desconectar WhatsApp', async () => {
    const { user } = setup(<SettingsScreen />);

    await user.click(screen.getByText(/WhatsApp/i));
    await user.click(screen.getByText(/Desconectar/i));

    expect(screen.getByText(/Desconectar WhatsApp?/i)).toBeInTheDocument();
  });

  it('deve salvar horários de atendimento', async () => {
    const { user } = setup(<WorkingHoursScreen />);

    const mondayCheckbox = screen.getAllByRole('checkbox')[1];
    await user.click(mondayCheckbox);

    await user.click(screen.getByText(/Salvar Horários/i));

    await waitFor(() => {
      expect(screen.getByText(/salvos!/i)).toBeInTheDocument();
    });
  });
});
```

---

## 7. Dependências

```json
{
  "dependencies": {
    "react": "^18.2.0",
    "react-router-dom": "^6.20.0",
    "lucide-react": "^0.300.0",
    "date-fns": "^3.0.0"
  }
}
```

---

**Desenvolvido para Agenda Cheia - Growth Layer para Salões de Beleza**
Versão 1.0 • Janeiro 2025
