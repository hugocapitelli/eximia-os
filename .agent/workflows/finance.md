---
description: Execute the The_CFO agent to process financial documents (OCR + Categorization).
---

1. Install dependencies (using py launcher to avoid PATH issues)
// turbo
py -m pip install -r FINANCE/Agentes/requirements.txt

2. Run the Finance Agent
py FINANCE/Agentes/cfo_agent.py
